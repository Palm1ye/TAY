<Page
    x:Class="TAY.Views.CleanerView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:viewmodels="using:TAY.ViewModels"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    mc:Ignorable="d"
    Background="Transparent">
    
    <Page.Transitions>
        <TransitionCollection>
            <EntranceThemeTransition/>
        </TransitionCollection>
    </Page.Transitions>

    <Grid Margin="30,20,30,30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Page Header -->
        <StackPanel Margin="4,0,4,16">
            <TextBlock Text="Temporary files and cache" FontFamily="Segoe UI" FontSize="12" Foreground="{StaticResource TextDim}"/>
            <TextBlock Text="Cleaner" FontFamily="Segoe UI" FontSize="28" FontWeight="SemiBold" Foreground="{StaticResource TextMain}" Margin="0,2,0,0"/>
        </StackPanel>
        
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="1.2*"/>
            </Grid.ColumnDefinitions>

            <!-- Directories ListView (Sleek terminal rows) -->
            <ListView ItemsSource="{x:Bind ViewModel.Folders}" SelectionMode="None" Margin="0,0,16,0">
                <ListView.Header>
                    <Grid Margin="16,0,16,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="120"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Cleanup targets" FontFamily="Segoe UI" FontSize="12" FontWeight="SemiBold" Foreground="{StaticResource TextMuted}" Grid.Column="1" Margin="16,0,0,0"/>
                        <TextBlock Text="Size" FontFamily="Segoe UI" FontSize="12" FontWeight="SemiBold" Foreground="{StaticResource TextMuted}" Grid.Column="2" HorizontalAlignment="Right"/>
                    </Grid>
                </ListView.Header>
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="viewmodels:FolderItemVM">
                        <Border Background="{StaticResource RowBg}" CornerRadius="4" Padding="14,8" Margin="4,0,4,6" BorderThickness="1" BorderBrush="{StaticResource RowBorder}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="120"/>
                                </Grid.ColumnDefinitions>
                                
                                <CheckBox IsChecked="{x:Bind IsSelected, Mode=TwoWay}" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,16,0" MinWidth="0"/>
                                
                                <StackPanel VerticalAlignment="Center" Grid.Column="1">
                                    <TextBlock Text="{x:Bind Name}" FontFamily="Consolas" FontSize="12" FontWeight="Bold" Foreground="{StaticResource TextMain}"/>
                                    <TextBlock Text="{x:Bind Path}" FontFamily="Consolas" FontSize="10" Foreground="{StaticResource TextMuted}" TextTrimming="CharacterEllipsis" Margin="0,3,0,0"/>
                                </StackPanel>
                                
                                <TextBlock Text="{x:Bind SizeStr}" FontFamily="Consolas" FontSize="12" FontWeight="Bold" Foreground="{StaticResource AccentMain}" VerticalAlignment="Center" Grid.Column="2" HorizontalAlignment="Right"/>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

            <!-- Summary Bento Card -->
            <Border Style="{StaticResource SweetCard}" Grid.Column="1" VerticalAlignment="Top" Padding="20">
                <StackPanel>
                    <TextBlock Text="Cleanup Summary" FontFamily="Segoe UI" FontSize="13" FontWeight="SemiBold" Foreground="{StaticResource TextMain}"/>
                    
                    <Grid Margin="0,16,0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="{x:Bind ViewModel.TotalSize, Mode=OneWay}" FontFamily="Consolas" FontSize="28" FontWeight="Bold" Foreground="{StaticResource TextMain}" Grid.Column="0" VerticalAlignment="Center"/>
                        <ProgressRing IsActive="{x:Bind ViewModel.IsScanning, Mode=OneWay}" Width="20" Height="20" Foreground="{StaticResource AccentMain}" Grid.Column="1" VerticalAlignment="Center"/>
                    </Grid>

                    <StackPanel Margin="0,0,0,18" Spacing="4">
                        <TextBlock Text="{x:Bind ViewModel.SelectedSize, Mode=OneWay}" FontFamily="Consolas" FontSize="12" FontWeight="Bold" Foreground="{StaticResource AccentMain}"/>
                        <TextBlock Text="{x:Bind ViewModel.SelectedCount, Mode=OneWay}" FontFamily="Segoe UI" FontSize="12" Foreground="{StaticResource TextDim}"/>
                        <TextBlock Text="{x:Bind ViewModel.CleanupStatus, Mode=OneWay}" FontFamily="Segoe UI" FontSize="11" Foreground="{StaticResource TextMuted}" TextWrapping="Wrap" Margin="0,6,0,0"/>
                    </StackPanel>

                    <Grid Margin="0,0,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Button Content="Select All" Style="{StaticResource OutlineButton}" Click="SelectAll_Click" Grid.Column="0" Margin="0,0,8,0"/>
                        <Button Content="Clear All" Style="{StaticResource OutlineButton}" Click="ClearAll_Click" Grid.Column="1" Margin="8,0,0,0"/>
                    </Grid>

                    <StackPanel Margin="0,0,0,12" Visibility="{x:Bind ViewModel.IsCleaning, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}">
                        <TextBlock Text="cleanup_in_progress" FontFamily="Consolas" FontSize="9" Foreground="{StaticResource AccentDanger}"/>
                        <ProgressBar IsIndeterminate="True" Height="2" Foreground="{StaticResource AccentDanger}" Background="#20F47C7C"/>
                    </StackPanel>

                    <!-- Primary Action Button -->
                    <Button Content="{x:Bind ViewModel.ScanButtonText, Mode=OneWay}" Command="{x:Bind ViewModel.ScanCommand}" IsEnabled="{x:Bind ViewModel.IsNotBusy, Mode=OneWay}" Style="{StaticResource SweetButton}" HorizontalAlignment="Stretch" Margin="0,0,0,12"/>
                    
                    <!-- Outlined/Soft Destructive Reclaim Button -->
                    <Button Content="{x:Bind ViewModel.CleanButtonText, Mode=OneWay}" Click="CleanSelected_Click" IsEnabled="{x:Bind ViewModel.IsNotBusy, Mode=OneWay}" Style="{StaticResource DestructiveOutlineButton}" HorizontalAlignment="Stretch"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Page>

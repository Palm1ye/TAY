<Page
    x:Class="TAY.Views.CleanerView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:viewmodels="using:TAY.ViewModels"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    mc:Ignorable="d"
    Background="Transparent">

    <Page.Transitions>
        <TransitionCollection>
            <EntranceThemeTransition />
        </TransitionCollection>
    </Page.Transitions>

    <Grid Margin="14,14,14,12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel>
                <TextBlock Text="Cleaner" FontSize="21" FontWeight="SemiBold" />
                <TextBlock Text="Reclaim disk space safely" FontSize="11" Foreground="{StaticResource TextMuted}" />
            </StackPanel>
            <Button Grid.Column="1" Style="{StaticResource GhostButton}" Command="{x:Bind ViewModel.ScanCommand}" IsEnabled="{x:Bind ViewModel.IsNotBusy, Mode=OneWay}" VerticalAlignment="Center">
                <StackPanel Orientation="Horizontal" Spacing="7">
                    <FontIcon Glyph="&#xE72C;" FontSize="13" />
                    <TextBlock Text="Rescan" />
                </StackPanel>
            </Button>
        </Grid>

        <Border Grid.Row="1" Style="{StaticResource SweetCard}" Margin="0,20,0,12" Padding="14">
            <Grid ColumnSpacing="14">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="68" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Border Width="58" Height="58" CornerRadius="29" BorderBrush="{StaticResource AccentMain}" BorderThickness="6" VerticalAlignment="Center">
                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="-1">
                        <TextBlock Text="{x:Bind ViewModel.TotalSize, Mode=OneWay}" FontSize="13" FontWeight="SemiBold" HorizontalAlignment="Center" />
                        <TextBlock Text="selected" FontSize="9" Foreground="{StaticResource TextMuted}" HorizontalAlignment="Center" />
                    </StackPanel>
                </Border>
                <StackPanel Grid.Column="1" Spacing="10">
                    <StackPanel Spacing="2">
                        <TextBlock Text="Ready to clean" FontSize="15" FontWeight="SemiBold" />
                        <TextBlock Text="{x:Bind ViewModel.SelectedCount, Mode=OneWay}" FontSize="12" Foreground="{StaticResource TextDim}" />
                    </StackPanel>
                    <Grid ColumnSpacing="8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="84" />
                        </Grid.ColumnDefinitions>
                        <Button Content="{x:Bind ViewModel.CleanButtonText, Mode=OneWay}" Style="{StaticResource SweetButton}" Click="CleanSelected_Click" IsEnabled="{x:Bind ViewModel.IsNotBusy, Mode=OneWay}" HorizontalAlignment="Stretch" />
                        <Button Grid.Column="1" Content="Preview" Style="{StaticResource OutlineButton}" Click="Preview_Click" />
                    </Grid>
                </StackPanel>
            </Grid>
        </Border>

        <Grid Grid.Row="2">
            <ListView ItemsSource="{x:Bind ViewModel.Folders}"
                      SelectionMode="None"
                      Padding="0,0,0,10"
                      ItemContainerStyle="{StaticResource FlatListViewItemStyle}">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="viewmodels:FolderItemVM">
                        <Border Background="{StaticResource RowBg}" BorderBrush="{StaticResource RowBorder}" BorderThickness="0,0,0,1" Padding="14,11">
                            <Grid ColumnSpacing="12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="30" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <CheckBox IsChecked="{x:Bind IsSelected, Mode=TwoWay}" MinWidth="0" VerticalAlignment="Center" />
                                <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="2">
                                    <TextBlock Text="{x:Bind Name}" FontSize="14" />
                                    <TextBlock Text="{x:Bind Path}" FontSize="11" Foreground="{StaticResource TextMuted}" TextTrimming="CharacterEllipsis" />
                                </StackPanel>
                                <TextBlock Grid.Column="2" Text="{x:Bind SizeStr}" FontSize="12" FontWeight="SemiBold" VerticalAlignment="Center" />
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

            <Border Background="#D8202020"
                    Visibility="{x:Bind ViewModel.IsCleaning, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}">
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="12">
                    <ProgressRing IsActive="{x:Bind ViewModel.IsCleaning, Mode=OneWay}" Width="34" Height="34" Foreground="{StaticResource AccentMain}" />
                    <TextBlock Text="{x:Bind ViewModel.CleanupStatus, Mode=OneWay}" FontSize="12" Foreground="{StaticResource TextDim}" TextWrapping="Wrap" MaxWidth="330" TextAlignment="Center" />
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Page>

<Window
    x:Class="TAY.Views.ActivityWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="TAY Activity">

    <Grid Background="{StaticResource BgSurface}" Padding="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel>
                <TextBlock Text="Activity" FontSize="20" FontWeight="SemiBold" />
                <TextBlock Text="Live events from this session" FontSize="11" Foreground="{StaticResource TextMuted}" />
            </StackPanel>
            <Button Grid.Column="1" Content="Clear" Style="{StaticResource GhostButton}" Click="Clear_Click" VerticalAlignment="Center" />
        </Grid>

        <Border Grid.Row="1" Style="{StaticResource SweetCard}" Padding="0">
            <ListView x:Name="ActivityList"
                      ItemsSource="{x:Bind LogLines, Mode=OneWay}"
                      SelectionMode="None"
                      ItemContainerStyle="{StaticResource FlatListViewItemStyle}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="{StaticResource RowBorder}" BorderThickness="0,0,0,1" Padding="14,10">
                            <Grid ColumnSpacing="12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="8" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <Ellipse Width="6" Height="6" Fill="{StaticResource AccentGreen}" VerticalAlignment="Center" />
                                <TextBlock Grid.Column="1" Text="{Binding}" FontSize="12" Foreground="{StaticResource TextDim}" TextWrapping="Wrap" />
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Border>
    </Grid>
</Window>

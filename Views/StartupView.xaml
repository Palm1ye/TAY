<Page
    x:Class="TAY.Views.StartupView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:viewmodels="using:TAY.ViewModels"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    mc:Ignorable="d"
    Background="Transparent">

    <Page.Transitions>
        <TransitionCollection>
            <EntranceThemeTransition />
        </TransitionCollection>
    </Page.Transitions>

    <Grid Margin="14,14,14,12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel>
                <TextBlock Text="Startup" FontSize="21" FontWeight="SemiBold" />
                <TextBlock Text="{x:Bind ViewModel.Guidance, Mode=OneWay}" FontSize="11" Foreground="{StaticResource TextMuted}" TextTrimming="CharacterEllipsis" />
            </StackPanel>
            <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                <Button Style="{StaticResource GhostButton}" Command="{x:Bind ViewModel.SortByImpactCommand}">
                    <StackPanel Orientation="Horizontal" Spacing="7">
                        <FontIcon Glyph="&#xE71C;" FontSize="13" />
                        <TextBlock Text="Filter" />
                    </StackPanel>
                </Button>
                <Button Style="{StaticResource GhostButton}" Command="{x:Bind ViewModel.SortByEnabledCommand}">
                    <StackPanel Orientation="Horizontal" Spacing="7">
                        <FontIcon Glyph="&#xE8CB;" FontSize="13" />
                        <TextBlock Text="Sort" />
                    </StackPanel>
                </Button>
            </StackPanel>
        </Grid>

        <Border Grid.Row="1" Style="{StaticResource SweetCard}" Margin="0,20,0,12" Padding="14">
            <StackPanel Spacing="12">
                <Grid ColumnSpacing="14">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="58" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Border Width="56" Height="56" CornerRadius="28" BorderBrush="{StaticResource AccentWarning}" BorderThickness="5">
                        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="-1">
                            <TextBlock Text="+6.5s" FontSize="16" FontWeight="SemiBold" HorizontalAlignment="Center" />
                            <TextBlock Text="boot" FontSize="9" Foreground="{StaticResource TextMuted}" HorizontalAlignment="Center" />
                        </StackPanel>
                    </Border>
                    <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="3">
                        <TextBlock Text="Estimated boot impact" FontSize="15" FontWeight="SemiBold" />
                        <TextBlock Text="Disabling high-impact enabled apps can reduce sign-in delay." FontSize="12" Foreground="{StaticResource TextDim}" TextWrapping="Wrap" />
                    </StackPanel>
                </Grid>

                <Button Content="Disable high-impact" Style="{StaticResource SweetButton}" Click="DisableHighImpact_Click" HorizontalAlignment="Stretch" />
            </StackPanel>
        </Border>

        <ListView Grid.Row="2"
                  ItemsSource="{x:Bind ViewModel.Apps}"
                  SelectionMode="None"
                  Padding="0,0,0,10"
                  ItemContainerStyle="{StaticResource FlatListViewItemStyle}">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="viewmodels:StartupAppVM">
                    <Border Background="{StaticResource RowBg}" BorderBrush="{StaticResource RowBorder}" BorderThickness="0,0,0,1" Padding="14,9">
                        <Grid ColumnSpacing="10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="34" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="52" />
                            </Grid.ColumnDefinitions>
                            <Border Width="30" Height="30" CornerRadius="4" Background="#333333" BorderBrush="{StaticResource CardBorder}" BorderThickness="1">
                                <TextBlock Text="{x:Bind Name}" FontSize="11" Foreground="{StaticResource TextDim}" HorizontalAlignment="Center" VerticalAlignment="Center" MaxWidth="18" TextTrimming="CharacterEllipsis" />
                            </Border>
                            <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="2">
                                <TextBlock Text="{x:Bind Name}" FontSize="14" TextTrimming="CharacterEllipsis" />
                                <TextBlock Text="{x:Bind Command}" FontSize="11" Foreground="{StaticResource TextMuted}" TextTrimming="CharacterEllipsis" />
                            </StackPanel>
                            <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="8" VerticalAlignment="Center" Margin="6,0">
                                <Ellipse Width="6" Height="6" Fill="{x:Bind ImpactBrush}" />
                                <TextBlock Text="{x:Bind Impact}" FontSize="11" Foreground="{x:Bind ImpactBrush}" />
                            </StackPanel>
                            <ToggleSwitch Grid.Column="3" IsOn="{x:Bind IsEnabled, Mode=TwoWay}" OnContent="" OffContent="" MinWidth="0" HorizontalAlignment="Right" VerticalAlignment="Center" />
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>

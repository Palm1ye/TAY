<Page
    x:Class="TAY.Views.StartupView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:viewmodels="using:TAY.ViewModels"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    mc:Ignorable="d"
    Background="Transparent">

    <Page.Transitions>
        <TransitionCollection>
            <EntranceThemeTransition/>
        </TransitionCollection>
    </Page.Transitions>

    <Grid Margin="30,20,30,30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/> <!-- Table Header -->
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Page Header -->
        <Grid Margin="4,0,4,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0">
                <TextBlock Text="STARTUP // BOOT_CONFIG" FontFamily="Consolas" FontSize="10" FontWeight="Bold" Foreground="{StaticResource TextMuted}" CharacterSpacing="140"/>
                <TextBlock Text="init_d.conf" FontFamily="Consolas" FontSize="20" FontWeight="Bold" Foreground="{StaticResource TextMain}" Margin="0,4,0,0"/>
            </StackPanel>
            <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                <Button Content="[ enable_all ]" Style="{StaticResource OutlineButton}" Click="EnableAll_Click"/>
                <Button Content="[ disable_all ]" Style="{StaticResource OutlineButton}" Click="DisableAll_Click"/>
            </StackPanel>
        </Grid>

        <!-- Table Header Row -->
        <Grid Grid.Row="1" Margin="16,0,16,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="150"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="// APP NAME / COMMAND" FontFamily="Consolas" FontSize="9" FontWeight="Bold" Foreground="{StaticResource TextMuted}" CharacterSpacing="80" Grid.Column="0"/>
            <TextBlock Text="// BOOT IMPACT" FontFamily="Consolas" FontSize="9" FontWeight="Bold" Foreground="{StaticResource TextMuted}" CharacterSpacing="80" Grid.Column="1"/>
            <TextBlock Text="// STARTUP STATUS" FontFamily="Consolas" FontSize="9" FontWeight="Bold" Foreground="{StaticResource TextMuted}" CharacterSpacing="80" Grid.Column="2" HorizontalAlignment="Right"/>
        </Grid>

        <!-- Startup ListView (Sleek terminal rows) -->
        <ListView ItemsSource="{x:Bind ViewModel.Apps}" SelectionMode="None" Grid.Row="2" Padding="0,0,0,16">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="viewmodels:StartupAppVM">
                    <!-- Dense Console output row -->
                    <Border Background="{StaticResource RowBg}" CornerRadius="4" Padding="12,4" Margin="4,0,4,3" BorderThickness="1" BorderBrush="{StaticResource RowBorder}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="2*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="150"/>
                            </Grid.ColumnDefinitions>
                            
                            <!-- Column 0: Name / Command in Monospace -->
                            <StackPanel Orientation="Horizontal" Grid.Column="0" VerticalAlignment="Center">
                                <Border Background="{StaticResource RowBgAlt}" CornerRadius="4" Width="28" Height="28" Margin="0,0,12,0" HorizontalAlignment="Center" VerticalAlignment="Center">
                                    <FontIcon Glyph="&#xE90F;" FontSize="12" Foreground="{StaticResource TextDim}" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                </Border>
                                <StackPanel VerticalAlignment="Center">
                                    <TextBlock Text="{x:Bind Name}" FontFamily="Consolas" FontSize="11" FontWeight="Bold" Foreground="{StaticResource TextMain}" TextTrimming="CharacterEllipsis"/>
                                    <TextBlock Text="{x:Bind Command}" FontFamily="Consolas" FontSize="9" Foreground="{StaticResource TextMuted}" TextTrimming="CharacterEllipsis" Margin="0,2,0,0"/>
                                </StackPanel>
                            </StackPanel>
                            
                            <!-- Column 1: Impact level -->
                            <TextBlock Text="{x:Bind Impact}" FontFamily="Consolas" FontSize="11" Foreground="{StaticResource AccentWarning}" VerticalAlignment="Center" Grid.Column="1"/>
                            
                            <!-- Column 2: Status Toggle Switch -->
                            <ToggleSwitch IsOn="{x:Bind IsEnabled, Mode=TwoWay}" Grid.Column="2" HorizontalAlignment="Right" VerticalAlignment="Center" OnContent="" OffContent="" MinWidth="0" Margin="0,0,-4,0"/>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>

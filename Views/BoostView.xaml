<Page
    x:Class="TAY.Views.BoostView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    mc:Ignorable="d"
    Background="Transparent">

    <Page.Transitions>
        <TransitionCollection>
            <EntranceThemeTransition />
        </TransitionCollection>
    </Page.Transitions>

    <ScrollViewer x:Name="BoostScrollViewer" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <StackPanel Margin="14,14,14,12" Spacing="10">
            <Grid>
                <StackPanel>
                    <TextBlock Text="Boost Tuning" FontSize="21" FontWeight="SemiBold" />
                    <TextBlock Text="Game, memory, DNS, privacy, and shell tuning" FontSize="11" Foreground="{StaticResource TextMuted}" />
                </StackPanel>
            </Grid>

            <Border Style="{StaticResource SweetCard}" Margin="0,10,0,0" Padding="14">
                <Grid ColumnSpacing="14">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="52" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <Border Width="50" Height="50" CornerRadius="8" Background="{StaticResource AccentMain}">
                        <FontIcon Glyph="&#xE7FC;" FontSize="24" Foreground="#101010" HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                    <StackPanel Grid.Column="1" Spacing="2" VerticalAlignment="Center">
                        <TextBlock Text="Game Focus Mode" FontSize="15" FontWeight="SemiBold" />
                        <TextBlock Text="{Binding ExpectedGameBoost}" FontSize="12" Foreground="{StaticResource TextDim}" TextWrapping="Wrap" MaxLines="2" />
                    </StackPanel>
                    <ToggleSwitch Grid.Column="2" IsOn="{Binding IsGameModeActive, Mode=TwoWay}" OnContent="" OffContent="" MinWidth="0" VerticalAlignment="Center" />
                </Grid>
            </Border>

            <Border Style="{StaticResource SweetCard}" Padding="14">
                <StackPanel Spacing="12">
                    <Grid ColumnSpacing="12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="42" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Border Width="40" Height="40" CornerRadius="7" Background="#333333" BorderBrush="{StaticResource CardBorder}" BorderThickness="1">
                            <FontIcon Glyph="&#xE8B7;" FontSize="17" Foreground="{StaticResource AccentMemory}" HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <StackPanel Grid.Column="1" Spacing="2">
                            <TextBlock Text="Memory Sweep" FontSize="15" FontWeight="SemiBold" />
                            <TextBlock Text="{Binding ExpectedMemoryBoost}" FontSize="12" Foreground="{StaticResource TextDim}" TextWrapping="Wrap" />
                        </StackPanel>
                    </Grid>
                    <Button Content="Deep clean RAM" Command="{Binding DeepCleanRamCommand}" Style="{StaticResource SweetButton}" HorizontalAlignment="Stretch" />
                    <Border Background="{StaticResource RowBgAlt}" BorderBrush="{StaticResource RowBorder}" BorderThickness="1" CornerRadius="5" Padding="10" Visibility="{Binding IsRamPanelVisible, Converter={StaticResource BoolToVisibilityConverter}, Mode=OneWay}">
                        <StackPanel Spacing="8">
                            <ProgressBar IsIndeterminate="{Binding IsRamCleaning, Mode=OneWay}" Height="3" Foreground="{StaticResource AccentMain}" />
                            <TextBlock Text="{Binding RamCleanLog, Mode=OneWay}" FontFamily="Consolas" FontSize="10" Foreground="{StaticResource TextDim}" TextWrapping="Wrap" />
                            <Button Content="OK" Command="{Binding DismissRamResultCommand}" Style="{StaticResource OutlineButton}" Visibility="{Binding IsRamCleaning, Converter={StaticResource InverseBoolToVisibilityConverter}, Mode=OneWay}" />
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Border>

            <Border Style="{StaticResource SweetCard}" Padding="14">
                <StackPanel Spacing="12">
                    <Grid ColumnSpacing="12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="42" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Border Width="40" Height="40" CornerRadius="7" Background="#333333" BorderBrush="{StaticResource CardBorder}" BorderThickness="1">
                            <FontIcon Glyph="&#xE839;" FontSize="17" Foreground="{StaticResource AccentPurple}" HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <StackPanel Grid.Column="1" Spacing="2">
                            <TextBlock Text="Network and DNS" FontSize="15" FontWeight="SemiBold" />
                            <TextBlock Text="{Binding DnsScanResult, Mode=OneWay}" FontSize="12" Foreground="{StaticResource TextDim}" TextWrapping="Wrap" />
                        </StackPanel>
                    </Grid>
                    <Grid ColumnSpacing="8" RowSpacing="8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <Button Content="Flush DNS" Command="{Binding FlushDnsCommand}" Style="{StaticResource OutlineButton}" HorizontalAlignment="Stretch" />
                        <Button Grid.Column="1" Content="Scan DNS" Command="{Binding FindBestDnsCommand}" Style="{StaticResource SweetButton}" HorizontalAlignment="Stretch" />
                        <Button Grid.Row="1" Content="Apply DNS" Click="ApplyRecommendedDns_Click" IsEnabled="{Binding CanApplyRecommendedDns, Mode=OneWay}" Style="{StaticResource OutlineButton}" HorizontalAlignment="Stretch" />
                        <Button Grid.Row="1" Grid.Column="1" Content="Restore DHCP" Command="{Binding RestoreDefaultDnsCommand}" Style="{StaticResource OutlineButton}" HorizontalAlignment="Stretch" />
                    </Grid>
                    <TextBlock Text="{Binding RecommendedDnsSummary, Mode=OneWay}" FontSize="11" Foreground="{StaticResource TextMuted}" TextWrapping="Wrap" />
                    <Border Background="{StaticResource RowBgAlt}" BorderBrush="{StaticResource RowBorder}" BorderThickness="1" CornerRadius="5" Padding="10" Visibility="{Binding IsNetworkCleaning, Converter={StaticResource BoolToVisibilityConverter}, Mode=OneWay}">
                        <TextBlock Text="{Binding NetworkCleanLog, Mode=OneWay}" FontFamily="Consolas" FontSize="10" Foreground="{StaticResource TextDim}" TextWrapping="Wrap" MaxLines="7" />
                    </Border>
                </StackPanel>
            </Border>

            <Border Style="{StaticResource SweetCard}" Padding="0">
                <StackPanel>
                    <Grid Padding="14,10">
                        <StackPanel>
                            <TextBlock Text="Privacy and drivers" FontSize="15" FontWeight="SemiBold" />
                            <TextBlock Text="Reversible Windows policy and signed driver checks" FontSize="11" Foreground="{StaticResource TextMuted}" />
                        </StackPanel>
                    </Grid>
                    <Rectangle Height="1" Fill="{StaticResource RowBorder}" />
                    <Grid Padding="14,11" ColumnSpacing="12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock Text="Privacy Shield" FontSize="14" />
                            <TextBlock Text="Telemetry and assistant background policies" FontSize="11" Foreground="{StaticResource TextMuted}" />
                        </StackPanel>
                        <ToggleSwitch Grid.Column="1" IsOn="{Binding IsTelemetryDebloated, Mode=TwoWay}" OnContent="" OffContent="" MinWidth="0" />
                    </Grid>
                    <Rectangle Height="1" Fill="{StaticResource RowBorder}" />
                    <Grid Padding="14,11" ColumnSpacing="12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock Text="{Binding OutdatedDriversCount, Mode=OneWay}" FontSize="23" FontWeight="SemiBold" Foreground="{StaticResource AccentWarning}" />
                            <TextBlock Text="drivers need review" FontSize="11" Foreground="{StaticResource TextMuted}" />
                        </StackPanel>
                        <Button Grid.Column="1" Content="Scan" Command="{Binding RefreshDriversCommand}" Style="{StaticResource OutlineButton}" VerticalAlignment="Center" />
                    </Grid>
                    <ItemsControl ItemsSource="{Binding Drivers}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border BorderBrush="{StaticResource RowBorder}" BorderThickness="0,1,0,0" Padding="14,9">
                                    <Grid ColumnSpacing="10">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <StackPanel Spacing="2">
                                            <TextBlock Text="{Binding DeviceName}" FontSize="13" TextTrimming="CharacterEllipsis" />
                                            <TextBlock Text="{Binding Provider}" FontSize="10" Foreground="{StaticResource TextMuted}" TextTrimming="CharacterEllipsis" />
                                            <TextBlock Text="{Binding Suggestion}" FontSize="10" Foreground="{StaticResource TextMuted}" TextWrapping="Wrap" MaxLines="2" />
                                        </StackPanel>
                                        <StackPanel Grid.Column="1" VerticalAlignment="Top" Spacing="4" MinWidth="82">
                                            <Border Background="{StaticResource AccentWarningSoft}" CornerRadius="10" Padding="7,3" Visibility="{Binding IsOutdated, Converter={StaticResource BoolToVisibilityConverter}}">
                                                <TextBlock Text="{Binding StatusLabel}" FontSize="10" Foreground="{StaticResource AccentWarning}" HorizontalAlignment="Center" />
                                            </Border>
                                            <Border Background="{StaticResource AccentGreenSoft}" CornerRadius="10" Padding="7,3" Visibility="{Binding IsOutdated, Converter={StaticResource InverseBoolToVisibilityConverter}}">
                                                <TextBlock Text="{Binding StatusLabel}" FontSize="10" Foreground="{StaticResource AccentGreen}" HorizontalAlignment="Center" />
                                            </Border>
                                            <TextBlock Text="{Binding DateLabel}" FontFamily="Consolas" FontSize="9" Foreground="{StaticResource TextMuted}" HorizontalAlignment="Center" />
                                        </StackPanel>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </Border>

            <Border Style="{StaticResource SweetCard}" Padding="0">
                <StackPanel>
                    <Grid Padding="14,10">
                        <TextBlock Text="Context menu" FontSize="15" FontWeight="SemiBold" />
                        <Button Content="Refresh" Command="{Binding RefreshContextHandlersCommand}" Style="{StaticResource GhostButton}" Padding="8,3" HorizontalAlignment="Right" />
                    </Grid>
                    <ItemsControl ItemsSource="{Binding ContextHandlers}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border BorderBrush="{StaticResource RowBorder}" BorderThickness="0,1,0,0" Padding="14,9">
                                    <Grid ColumnSpacing="10">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <StackPanel>
                                            <TextBlock Text="{Binding Name}" FontSize="13" TextTrimming="CharacterEllipsis" />
                                            <TextBlock Text="{Binding SourceType}" FontSize="10" Foreground="{StaticResource TextMuted}" />
                                        </StackPanel>
                                        <ToggleSwitch Grid.Column="1" IsOn="{Binding IsEnabled, Mode=OneWay}" Toggled="OnContextMenuToggled" OnContent="" OffContent="" MinWidth="0" />
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</Page>

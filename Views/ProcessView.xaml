<Page
    x:Class="TAY.Views.ProcessView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:viewmodels="using:TAY.ViewModels"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    mc:Ignorable="d"
    Background="Transparent">

    <Page.Transitions>
        <TransitionCollection>
            <EntranceThemeTransition />
        </TransitionCollection>
    </Page.Transitions>

    <Grid Margin="14,14,14,12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel>
                <TextBlock Text="Processes" FontSize="21" FontWeight="SemiBold" />
                <TextBlock Text="{x:Bind ViewModel.OperationStatus, Mode=OneWay}" FontSize="11" Foreground="{StaticResource TextMuted}" TextTrimming="CharacterEllipsis" />
            </StackPanel>
            <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                <Button Style="{StaticResource GhostButton}" Click="Filter_Click">
                    <StackPanel Orientation="Horizontal" Spacing="7">
                        <FontIcon Glyph="&#xE71C;" FontSize="13" />
                        <TextBlock Text="Filter" />
                    </StackPanel>
                </Button>
                <Button Style="{StaticResource GhostButton}" Command="{x:Bind ViewModel.LoadProcessesCommand}" IsEnabled="{x:Bind ViewModel.IsNotRefreshing, Mode=OneWay}">
                    <StackPanel Orientation="Horizontal" Spacing="7">
                        <FontIcon Glyph="&#xE72C;" FontSize="13" />
                        <TextBlock Text="Refresh" />
                    </StackPanel>
                </Button>
            </StackPanel>
        </Grid>

        <Grid Grid.Row="1" Margin="12,24,12,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="70" />
                <ColumnDefinition Width="78" />
            </Grid.ColumnDefinitions>
            <TextBlock Text="NAME" FontSize="11" FontWeight="SemiBold" Foreground="{StaticResource TextMuted}" />
            <TextBlock Grid.Column="1" Text="CPU" FontSize="11" FontWeight="SemiBold" Foreground="{StaticResource AccentMain}" HorizontalAlignment="Center" />
            <TextBlock Grid.Column="2" Text="MEM" FontSize="11" FontWeight="SemiBold" Foreground="{StaticResource TextMuted}" HorizontalAlignment="Center" />
        </Grid>

        <ListView Grid.Row="2"
                  ItemsSource="{x:Bind ViewModel.Processes}"
                  SelectionMode="None"
                  Padding="0,0,0,10"
                  ItemContainerStyle="{StaticResource FlatListViewItemStyle}">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="viewmodels:ProcessItemVM">
                    <Border Background="{StaticResource RowBg}" BorderBrush="{StaticResource RowBorder}" BorderThickness="0,0,0,1" Padding="12,8">
                        <Grid ColumnSpacing="10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="34" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="70" />
                                <ColumnDefinition Width="78" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <Border Width="28" Height="28" CornerRadius="5" Background="#333333" BorderBrush="{StaticResource CardBorder}" BorderThickness="1">
                                <FontIcon Glyph="&#xE9D2;" FontSize="12" Foreground="{StaticResource TextDim}" HorizontalAlignment="Center" VerticalAlignment="Center" />
                            </Border>
                            <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="1">
                                <TextBlock Text="{x:Bind Name}" FontSize="13" TextTrimming="CharacterEllipsis" />
                                <TextBlock Text="{x:Bind Id}" FontSize="10" Foreground="{StaticResource TextMuted}" />
                            </StackPanel>
                            <StackPanel Grid.Column="2" VerticalAlignment="Center" Spacing="3">
                                <TextBlock Text="{x:Bind CpuStr}" FontSize="11" FontWeight="SemiBold" Foreground="{StaticResource AccentDanger}" HorizontalAlignment="Right" />
                                <ProgressBar Value="{x:Bind CpuValue}" Minimum="0" Maximum="100" Height="3" Foreground="{StaticResource AccentDanger}" Background="#3A3A3A" />
                            </StackPanel>
                            <StackPanel Grid.Column="3" VerticalAlignment="Center" Spacing="3">
                                <TextBlock Text="{x:Bind RamStr}" FontSize="11" FontWeight="SemiBold" HorizontalAlignment="Right" />
                                <ProgressBar Value="{x:Bind RamPercent}" Minimum="0" Maximum="100" Height="3" Foreground="{StaticResource TextDim}" Background="#3A3A3A" />
                            </StackPanel>
                            <Button Grid.Column="4" Style="{StaticResource GhostButton}" Click="EndTask_Click" IsEnabled="{x:Bind CanEndTask}" Padding="8,3">
                                <FontIcon Glyph="&#xE712;" FontSize="12" />
                            </Button>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
